'use strict';

define(function($delete, $export, $import, $inspect, $save) {
  return {
    'delete': $delete,
    export: $export,
    import: $import,
    inspect: $inspect,
    save: $save,
  };
});
